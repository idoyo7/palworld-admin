# CI 전용 파일 — 이미지 태그만 담는다. 사람이 편집하지 말 것.
# CI(docker-build-push.yml)가 push 마다 이 파일의 image.tag 만 갱신([skip ci]).
# 다른 설정은 chart/values.yaml(기본값) / mont-helm cvalues(환경 오버라이드)에서.
# → 태그 전용 파일 분리로 CI 커밋과 사람 편집이 절대 같은 파일을 안 건드림(rebase 충돌 원천 차단).
image:
  tag: "7b6c42"
